# Fix "request entity too large" when CMS uploads images.
# Add this inside the server { } that proxies to the Node API, then:
#   sudo nginx -t && sudo systemctl reload nginx

client_max_body_size 100M;
